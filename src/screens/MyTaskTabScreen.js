import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default MyTaskTabScreen = (props) => {
	return (
		<View>
			<Text>MyTaskTabScreen</Text>
		</View>
	)
}

const styles = StyleSheet.create({})
